<!DOCTYPE html>
<html lang="ar" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/errorStylesheet.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;500;600;700&display=swap"
    />
    <title><%= title %></title>
    <link rel="icon" href="/assets/images/favicon.ico" type="image/x-icon" />
  </head>
  <body>
    <main class="error-main">
      <div class="error-container">
        <div class="error-content">
          <div class="error-icon">
            <% if (status === 404) { %>
            <i class="fas fa-search"></i>
            <% } else if (status === 403) { %>
            <i class="fas fa-lock"></i>
            <% } else if (status === 500) { %>
            <i class="fas fa-exclamation-triangle"></i>
            <% } else { %>
            <i class="fas fa-exclamation-circle"></i>
            <% } %>
          </div>

          <div class="error-code"><%= status || 500 %></div>

          <div class="error-message">
            <h1><%= message || "خطأ غير معروف" %></h1>

            <p class="error-description">
              <% if (status === 404) { %> عذراً، الصفحة التي تبحث عنها غير
              موجودة أو تم نقلها إلى مكان آخر. <% } else if (status === 403) {
              %> ليس لديك الصلاحية للوصول إلى هذه الصفحة. <% } else if (status
              === 500) { %> نعتذر، حدث خطأ في الخادم. نحن نعمل على حل المشكلة.
              <% } else { %> حدث خطأ غير متوقع. يرجى المحاولة مرة أخرى لاحقاً.
              <% } %>
            </p>
          </div>

          <div class="error-actions">
            <a href="/" class="btn btn-primary">
              <i class="fas fa-home"></i>
              العودة للرئيسية
            </a>
          </div>
        </div>
      </div>
    </main>

    <footer>
      <div class="footer-bottom">
        <p class="copyright">&copy; 2025 مقارن. جميع الحقوق محفوظة.</p>
        <a
          href="https://red-shadows-rs.com"
          target="_blank"
          class="rs-logo-link"
        >
          <span class="rs-text">RED SHADOWS | RS</span>
          <img
            src="/assets/images/red-shadows-rs.png"
            alt="RED SHADOWS | RS"
            class="rs-logo"
          />
        </a>
      </div>
    </footer>
  </body>
</html>
